package net.battleships.content;

import java.io.File;

public class WeaponLoadable extends Loadable {
	WeaponLoadable(File srcFile, ContentLoader contentLoader) {
		super(srcFile, contentLoader);
	}
}
